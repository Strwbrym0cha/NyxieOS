import React,{useEffect,useMemo,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import './styles.css';
import Travel from './Travel.jsx';
import {YuuKun,Settings} from './MoreModules.jsx';
import Routines from './Routines.jsx';
import CreatorHQ from './CreatorHQ.jsx';
import TaskCrud from './TaskCrud.jsx';
import MoneyCrud from './MoneyCrud.jsx';
import Wellness from './Wellness.jsx';
import CosplayCrud from './CosplayCrud.jsx';
import ConventionCrud from './ConventionCrud.jsx';
import HomeCommand from './HomeCommand.jsx';
import {supabase} from './supabase.js';
import YuuChat from './YuuChat.jsx';
import Reminders from './Reminders.jsx';

const pad=n=>String(n).padStart(2,'0');
const localDate=(d=new Date())=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
const shiftDate=(date,days)=>{const d=new Date(`${date}T12:00:00`);d.setDate(d.getDate()+days);return localDate(d)};
const prettyDate=date=>new Intl.DateTimeFormat(undefined,{weekday:'long',month:'short',day:'numeric'}).format(new Date(`${date}T12:00:00`));
const defaults={creator:{items:[],collaborators:[],hashtagBank:[],archiveLinks:[]},wellness:{},routines:[],settings:{yuuEnabled:true,nagLevel:'Normal',displayName:'Nyxie',activeMode:'auto'},reminders:{manual:[],overrides:{}},yuu:{history:[],lastIntent:null},tasks:[{id:1,title:'Pack convention tote',date:localDate(),time:null,done:false,urgent:true},{id:2,title:'Reply to costume fabric seller',date:localDate(),time:null,done:false,urgent:false},{id:3,title:'Drink water + take a reset',date:localDate(),time:null,done:true,urgent:false},{id:4,title:'Photoshoot',date:localDate(),time:'14:00',done:false,urgent:true}],money:{availableToday:114.32,weeklyGoal:120,weeklyEarned:42,daysRemaining:6,earnedToday:0,buckets:{life:72,con:28,fun:14},transactions:[],workWindows:[],workWindowCheckins:{},upcoming:[],obligations:[],savingsGoals:[],debts:[],purchases:[],todayTarget:null},cosplay:{activeId:1,projects:[{id:1,name:'Star Guardian Nyxie',source:'Original design',state:'In Progress',targetEvent:'Galaxy Con',targetDate:'2026-09-20',budget:160,progress:58,pieces:[{id:1,name:'Jacket',method:'Make',cost:28,status:'In Progress',due:'2026-09-10',link:'',notes:''},{id:2,name:'Wig',method:'Buy',cost:34,status:'Ready',due:'2026-09-05',link:'',notes:''},{id:3,name:'Boot covers',method:'Commission',cost:45,status:'Planning',due:'2026-09-18',link:'',notes:''}]}]},conventions:{activeId:null,items:[]},travel:{activeId:null,trips:[],packingTemplates:[]}};
function normalizeTask(t,i){return {...t,id:t.id??Date.now()+i,date:t.date||localDate(),time:t.time||null,done:Boolean(t.done),urgent:Boolean(t.urgent)}}
function normalizeYuu(value){const y=value||{};return {...defaults.yuu,...y,history:Array.isArray(y.history)?y.history.slice(-50):[],lastIntent:y.lastIntent||null}}
function migrateCosplay(old){if(old?.projects){const p=old.projects.find(x=>x.id===old.activeId)||old.projects[0];return {...defaults.cosplay,...old,name:p?.name,progress:p?.progress,pieces:p?.pieces||[]}}if(old?.name){const p={...defaults.cosplay.projects[0],...old,pieces:(old.pieces||[]).map((x,i)=>({...x,id:x.id??i+1,method:x.method||({make:'Make',buy:'Buy',commission:'Commission'}[x.mode]||'Make'),status:x.status||'Planning',cost:Number(x.cost)||0,link:x.link||'',notes:x.notes||''}))};return {...defaults.cosplay,activeId:p.id,projects:[p],name:p.name,progress:p.progress,pieces:p.pieces}}return defaults.cosplay}
function mergeData(raw){const old=raw||{},m=old.money||{};return {...defaults,...old,conventions:{...defaults.conventions,...(old.conventions||{}),items:Array.isArray(old.conventions?.items)?old.conventions.items:[]},tasks:(Array.isArray(old.tasks)?old.tasks:defaults.tasks).map(normalizeTask),creator:{...defaults.creator,...(old.creator||{})},wellness:old.wellness||{},routines:Array.isArray(old.routines)?old.routines:[],settings:{...defaults.settings,...(old.settings||{})},yuu:normalizeYuu(old.yuu),reminders:{...defaults.reminders,...(old.reminders||{}),manual:Array.isArray(old.reminders?.manual)?old.reminders.manual:[],overrides:old.reminders?.overrides&&typeof old.reminders.overrides==='object'?old.reminders.overrides:{}},travel:{activeId:old.travel?.activeId??null,trips:Array.isArray(old.travel?.trips)?old.travel.trips:[],packingTemplates:Array.isArray(old.travel?.packingTemplates)?old.travel.packingTemplates:[]},money:{...defaults.money,weeklyEarned:old.earnings??defaults.money.weeklyEarned,weeklyGoal:old.goal??defaults.money.weeklyGoal,...m,buckets:{...defaults.money.buckets,...(m.buckets||{})},transactions:Array.isArray(m.transactions)?m.transactions:defaults.money.transactions,workWindows:Array.isArray(m.workWindows)?m.workWindows:defaults.money.workWindows,upcoming:Array.isArray(m.upcoming)?m.upcoming:defaults.money.upcoming,workWindowCheckins:m.workWindowCheckins&&typeof m.workWindowCheckins==='object'?m.workWindowCheckins:{},obligations:Array.isArray(m.obligations)?m.obligations:[],savingsGoals:Array.isArray(m.savingsGoals)?m.savingsGoals:[],debts:Array.isArray(m.debts)?m.debts:[],purchases:Array.isArray(m.purchases)?m.purchases:[],todayTarget:m.todayTarget??null},cosplay:migrateCosplay(old.cosplay)}}
const getActiveCosplay=c=>c?.projects?.find(p=>p.id===c.activeId)||null;
const getCosplayProgress=p=>p?.pieces?.length?Math.round(p.pieces.filter(x=>x.status==='Ready').length/p.pieces.length*100):Number(p?.progress||0);
const getNextCosplayPiece=p=>(p?.pieces||[]).filter(x=>x.status!=='Ready').sort((a,b)=>(a.due||'9999').localeCompare(b.due||'9999'))[0]||null;
const getNearestConvention=(items=[])=>items.filter(x=>x.startDate&&x.startDate>=localDate()&&x.status!=='Completed').sort((a,b)=>a.startDate.localeCompare(b.startDate))[0]||null;
const countdown=c=>{if(!c?.startDate)return '';const d=Math.ceil((new Date(c.startDate+'T12:00:00')-new Date(localDate()+'T12:00:00'))/86400000);return d<=0?'Today':d+' days'};
function useStore(){
 const [data,setData]=useState(()=>{try{return mergeData(JSON.parse(localStorage.getItem('nyxie-data')))}catch{return mergeData(null)}});
 const [session,setSession]=useState(null);
 const [cloudStatus,setCloudStatus]=useState('Local only');
 const [cloudNotice,setCloudNotice]=useState('');
 const dataRef=useRef(data);
 const sessionRef=useRef(null);
 const bootstrapDoneRef=useRef(false);
 const bootstrapInFlightRef=useRef(false);
 const bootstrapUserRef=useRef(null);
 const lastSyncedRef=useRef('');
 const revisionRef=useRef(null);
 const saveTimerRef=useRef(null);
 const saveVersionRef=useRef(0);

 useEffect(()=>{dataRef.current=data;localStorage.setItem('nyxie-data',JSON.stringify(data))},[data]);

 useEffect(()=>{
   let disposed=false;
   const clearSaveTimer=()=>{if(saveTimerRef.current){clearTimeout(saveTimerRef.current);saveTimerRef.current=null}};
   const syncSession=async(nextSession)=>{
     if(disposed)return;
     const user=nextSession?.user||null;
     const userId=user?.id||null;
     const currentId=sessionRef.current?.user?.id||null;
     if(userId&&userId===currentId&&(bootstrapInFlightRef.current||bootstrapDoneRef.current))return;
     clearSaveTimer();
     saveVersionRef.current+=1;
     sessionRef.current=nextSession||null;
     setSession(nextSession||null);
     bootstrapUserRef.current=userId;
     bootstrapDoneRef.current=false;
     bootstrapInFlightRef.current=false;
     lastSyncedRef.current='';
     setCloudNotice('');
     if(!userId){
       setCloudStatus('Local only');
       return;
     }
     bootstrapInFlightRef.current=true;
     setCloudStatus('Loading cloud data');
     const localData=mergeData(dataRef.current);
     try{
       const result=await supabase.from('nyxie_planner_data').select('data,revision').eq('user_id',userId).maybeSingle();
       if(sessionRef.current?.user?.id!==userId)return;
       if(result.error)throw result.error;
       if(result.data?.data){
         const normalized=mergeData(result.data.data);
         lastSyncedRef.current=JSON.stringify(normalized);
         revisionRef.current=result.data.revision??null;
         setData(normalized);
       }else{
         const initial=mergeData(localData);
         const inserted=await supabase.from('nyxie_planner_data').upsert({user_id:userId,data:initial},{onConflict:'user_id'}).select('revision').single();
         if(sessionRef.current?.user?.id!==userId)return;
         if(inserted.error)throw inserted.error;
         lastSyncedRef.current=JSON.stringify(initial);
         revisionRef.current=inserted.data?.revision??null;
       }
       bootstrapDoneRef.current=true;
       setCloudStatus('Saved to cloud');
     }catch(error){
       if(sessionRef.current?.user?.id===userId){
         console.error('NyxieOS cloud bootstrap failed',error);
         setCloudStatus('Save error / Offline');
         setCloudNotice('Couldn’t reach cloud storage. Your device copy is still safe.');
       }
     }finally{
       if(sessionRef.current?.user?.id===userId)bootstrapInFlightRef.current=false;
     }
   };
   supabase.auth.getSession().then(({data:result,error})=>{
     if(error)throw error;
     return syncSession(result.session);
   }).catch(error=>{
     if(!disposed){
       console.error('NyxieOS auth session check failed',error);
       setCloudStatus('Local only');
     }
   });
   const {data:authData}=supabase.auth.onAuthStateChange((event,nextSession)=>{
     if(event==='INITIAL_SESSION'||event==='SIGNED_IN'||event==='SIGNED_OUT')syncSession(nextSession);
   });
   return()=>{disposed=true;clearSaveTimer();saveVersionRef.current+=1;authData.subscription.unsubscribe()};
 },[]);

 useEffect(()=>{
   if(!session?.user?.id||!bootstrapDoneRef.current)return;
   const serialized=JSON.stringify(data);
   if(serialized===lastSyncedRef.current)return;
   if(saveTimerRef.current)clearTimeout(saveTimerRef.current);
   const version=++saveVersionRef.current;
   setCloudStatus('Syncing…');
   setCloudNotice('');
   saveTimerRef.current=setTimeout(async()=>{
     const userId=session.user.id;
     const result=await supabase.from('nyxie_planner_data').update({data}).eq('user_id',userId).select('revision').maybeSingle();
     if(result.error){
       if(version===saveVersionRef.current&&sessionRef.current?.user?.id===userId){
         console.error('NyxieOS cloud save failed',result.error);
         setCloudStatus('Save error / Offline');
         setCloudNotice('Couldn’t reach cloud storage. Your device copy is still safe.');
       }
       return;
     }
     if(version!==saveVersionRef.current||sessionRef.current?.user?.id!==userId)return;
     lastSyncedRef.current=serialized;
     revisionRef.current=result.data?.revision??revisionRef.current;
     setCloudStatus('Saved to cloud');
   },700);
   return()=>{if(saveTimerRef.current)clearTimeout(saveTimerRef.current)};
 },[data,session]);

 const sendMagicLink=async(email)=>{
   const clean=String(email||'').trim();
   if(!clean){setCloudNotice('Enter your email to continue.');return false}
   setCloudStatus('Sending magic link…');
   setCloudNotice('');
   try{
     const redirect=window.location.origin+window.location.pathname;
     const result=await supabase.auth.signInWithOtp({email:clean,options:{emailRedirectTo:redirect}});
     if(result.error)throw result.error;
     setCloudStatus('Check your email ✉️');
     setCloudNotice('Check your email ✉️');
     return true;
   }catch(error){
     console.error('NyxieOS magic link failed',error);
     setCloudStatus(sessionRef.current?'Saved to cloud':'Local only');
     setCloudNotice('Magic link couldn’t be sent. Try again.');
     return false;
   }
 };
 const signOut=async()=>{
   const result=await supabase.auth.signOut();
   if(result.error){
     console.error('NyxieOS sign out failed',result.error);
     setCloudStatus('Save error / Offline');
     setCloudNotice('Couldn’t sign out cleanly. Try again.');
   }
 };
 const cloud=useMemo(()=>({status:cloudStatus,notice:cloudNotice,email:session?.user?.email||'',session,sendMagicLink,signOut}),[cloudStatus,cloudNotice,session]);
 return [data,setData,cloud];
}const nav=[['home','⌂','Home'],['plan','✦','Plan'],['money','♡','Money'],['cosplay','✿','Cosplay'],['more','⋯','More']];
function MagicCard({children,className='',onClick}){return <section className={'card '+className} onClick={onClick}>{children}</section>}
function SectionHeader({title,action}){return <div className="section-head"><h2>{title}</h2>{action&&<span>{action}</span>}</div>}
function ProgressSpell({value,label}){return <div className="progress-wrap"><div className="progress-label"><span>{label}</span><strong>{Math.round(value)}%</strong></div><div className="progress"><i style={{width:`${Math.min(100,Math.max(0,value))}%`}}/></div></div>}
function YuuBubble({children}){return <div className="yuu"><div className="yuu-avatar">Y</div><div><b>Yuu-Kun</b><p>{children}</p></div></div>}
function UrgentChip(){return <span className="urgent">Urgent</span>}
function QuickAdd({onAdd}){const [v,setV]=useState('');return <form className="quick-add" onSubmit={e=>{e.preventDefault();if(v.trim()){onAdd(v.trim());setV('')}}}><input aria-label="Task title" value={v} onChange={e=>setV(e.target.value)} placeholder="Add an anytime task…"/><button aria-label="Add task">+</button></form>}
function BottomNav({screen,setScreen}){return <nav className="bottom-nav">{nav.map(([id,icon,label])=><button className={(screen===id||(id==='more'&&['travel','conventions','creator','wellness','routines','yuu','settings'].includes(screen)))?'active':''} onClick={()=>setScreen(id)} key={id}><span>{icon}</span>{label}</button>)}</nav>}
function AppShell({screen,setScreen,children}){return <div className="app"><main>{children}</main><BottomNav screen={screen} setScreen={setScreen}/></div>}
function TaskRow({task,onToggle,onMove}){return <label className={task.done?'done task-row':'task-row'}><input type="checkbox" checked={task.done} onChange={onToggle}/><span className="task-copy">{task.title}{task.time&&<small>{new Date(`2000-01-01T${task.time}`).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}</small>}</span>{task.urgent&&<UrgentChip/>}{onMove&&!task.done&&<button type="button" className="tiny-action" onClick={e=>{e.preventDefault();onMove()}}>Tomorrow</button>}</label>}
function Home({data,setData,setScreen}){const today=localDate(),tasks=data.tasks.filter(t=>t.date===today),m=data.money,remaining=Math.max(0,m.weeklyGoal-m.weeklyEarned),hour=new Date().getHours(),greeting=hour<12?'Good morning':hour<18?'Good afternoon':'Good evening',date=new Intl.DateTimeFormat(undefined,{weekday:'long',month:'long',day:'numeric'}).format(new Date());return <><header><div><small>{date}</small><h1>{greeting}, Nyxie ✨</h1></div><div className="sparkle">✦</div></header><YuuBubble>Yare yare. {tasks.filter(t=>!t.done).length} things today.</YuuBubble><SectionHeader title="Today" action="Anytime today"/><MagicCard><div className="task-list">{tasks.map(t=><TaskRow key={t.id} task={t} onToggle={()=>setData({...data,tasks:data.tasks.map(x=>x.id===t.id?{...x,done:!x.done}:x)})}/>)}</div><QuickAdd onAdd={title=>setData({...data,tasks:[...data.tasks,{id:Date.now(),title,date:today,time:null,done:false,urgent:false}]})}/></MagicCard><SectionHeader title="Money Today"/><MagicCard className="money-home" onClick={()=>setScreen('money')}><div className="money-home-top"><div><small>Available today</small><strong>${m.availableToday.toFixed(2)}</strong></div><span className="arrow">→</span></div><div className="money-home-earned"><span>Earned today</span><b>${Number(m.earnedToday||0).toFixed(2)}</b></div><ProgressSpell value={m.weeklyGoal?m.weeklyEarned/m.weeklyGoal*100:0} label={`Weekly Mission · ${m.weeklyEarned} / ${m.weeklyGoal}`}/><div className="money-home-bottom"><span>${remaining} left</span><span>Suggested today: ${Math.ceil(remaining/Math.max(1,m.daysRemaining))}</span></div></MagicCard><SectionHeader title="Current Cosplay"/><MagicCard className="cosplay-home" onClick={()=>setScreen('cosplay')}><div className="cosplay-summary"><div className="project-icon">✿</div><div><b>{data.cosplay.name}</b><small>Galaxy Con · due Sep 18</small></div><span>→</span></div><ProgressSpell value={data.cosplay.progress} label="Project progress"/><p className="next-piece">Next: Jacket · In progress</p></MagicCard><SectionHeader title="Coming Up"/><MagicCard className="coming-up">{(()=>{const c=getNearestConvention(data.conventions?.items);return c?<><b>{c.name}</b><small>{countdown(c)} · {c.startDate}{c.location?" · "+c.location:""}</small></>:<b>No upcoming conventions yet.</b>})()}</MagicCard></>}
function Plan({data,setData}){const [mode,setMode]=useState('today'),[selected,setSelected]=useState(localDate()),[showTime,setShowTime]=useState(false),[time,setTime]=useState('');const tasks=data.tasks,dayTasks=tasks.filter(t=>t.date===selected),scheduled=dayTasks.filter(t=>t.time),anytime=dayTasks.filter(t=>!t.time);const addTask=(title,date=selected,taskTime=null)=>setData({...data,tasks:[...tasks,{id:Date.now(),title,date,time:taskTime,done:false,urgent:false}]});const toggle=id=>setData({...data,tasks:tasks.map(t=>t.id===id?{...t,done:!t.done}:t)});const addFromInput=title=>addTask(title,selected,showTime&&time?time:null);const days=Array.from({length:7},(_,i)=>shiftDate(localDate(),i));const month=selected.slice(0,7),monthDays=Array.from({length:new Date(Number(selected.slice(0,4)),Number(selected.slice(5,7)),0).getDate()},(_,i)=>`${month}-${pad(i+1)}`);const summary=<div className="plan-context"><b>{prettyDate(selected)}</b><span>{scheduled.length} scheduled · {anytime.length} anytime</span></div>;return <><header><div><small>Your day, without the time-block prison.</small><h1>Plan</h1></div></header><div className="segmented" role="tablist">{[['today','Today'],['week','Week'],['month','Month']].map(([id,label])=><button role="tab" aria-selected={mode===id} className={mode===id?'selected':''} onClick={()=>setMode(id)} key={id}>{label}</button>)}</div>{mode==='today'&&<>{summary}<SectionHeader title="Scheduled"/><MagicCard>{scheduled.length?<div className="schedule-list">{scheduled.map(t=><TaskRow key={t.id} task={t} onToggle={()=>toggle(t.id)}/>)}</div>:<p className="muted">Nothing with a clock time yet.</p>}</MagicCard><SectionHeader title="Anytime Today" action="No clock required"/><MagicCard><div className="task-list">{anytime.length?anytime.map(t=><TaskRow key={t.id} task={t} onToggle={()=>toggle(t.id)} onMove={()=>setData({...data,tasks:tasks.map(x=>x.id===t.id?{...x,date:shiftDate(t.date,1)}:x)})}/>):<p className="muted">Your flexible list is clear.</p>}</div><div className="add-time"><button className="secondary" onClick={()=>setShowTime(!showTime)}>{showTime?'Hide time':'Add a time intentionally'}</button>{showTime&&<input aria-label="Optional task time" type="time" value={time} onChange={e=>setTime(e.target.value)}/>}<QuickAdd onAdd={addFromInput}/></div></MagicCard></>}{mode==='week'&&<><div className="plan-context"><b>This week</b><span>Tap a day for its planning details</span></div><div className="day-stack">{days.map(day=>{const d=tasks.filter(t=>t.date===day),s=d.filter(t=>t.time),a=d.filter(t=>!t.time);return <button className={'day-card '+(day===selected?'selected':'')} onClick={()=>{setSelected(day);setMode('today')}} key={day}><b>{prettyDate(day)}</b><span>{s.length} scheduled · {a.length} anytime</span><small>{d.filter(t=>!t.done).slice(0,2).map(t=>t.title).join(' · ')||'Nothing pending'}</small></button>})}</div></>}{mode==='month'&&<><div className="plan-context"><b>{new Intl.DateTimeFormat(undefined,{month:'long',year:'numeric'}).format(new Date(`${selected}T12:00:00`))}</b><span>Dots mark days with plans</span></div><div className="month-grid">{monthDays.map(day=><button className={day===selected?'selected':''} onClick={()=>setSelected(day)} key={day}><b>{Number(day.slice(-2))}</b>{tasks.some(t=>t.date===day&&t.time)&&<i className="dot scheduled-dot"/>}{tasks.some(t=>t.date===day&&!t.time)&&<i className="dot anytime-dot"/>}</button>)}</div><SectionHeader title="Selected day"/><MagicCard><p className="muted">{prettyDate(selected)} · {dayTasks.length} items</p><button className="primary" onClick={()=>setMode('today')}>Open day details</button></MagicCard></>}</>}
function Money({data,setData}){const m=data.money,remaining=Math.max(0,m.weeklyGoal-m.weeklyEarned);return <><header><div><small>Build your week</small><h1>Money</h1></div></header><MagicCard className="hero-money"><small>Available Today</small><strong>${m.availableToday.toFixed(2)}</strong><p>Your actual spendable amount</p></MagicCard><SectionHeader title="Weekly Money Mission"/><MagicCard><ProgressSpell value={m.weeklyGoal?m.weeklyEarned/m.weeklyGoal*100:0} label="Weekly goal"/><div className="money-stats"><div><small>Earned</small><b>${m.weeklyEarned}</b></div><div><small>Remaining</small><b>${remaining}</b></div><div><small>Days left</small><b>{m.daysRemaining}</b></div></div><button className="primary" onClick={()=>setData({...data,money:{...m,weeklyEarned:Math.min(m.weeklyGoal,m.weeklyEarned+10),earnedToday:Number(m.earnedToday||0)+10}})}>Log $10 earned</button><button className="secondary" onClick={()=>setData({...data,money:{...m,weeklyGoal:m.weeklyGoal+20}})}>Raise weekly goal</button></MagicCard><SectionHeader title="Work window"/><MagicCard><p className="muted">A flexible place for future work sessions.</p><button className="secondary">Add a window</button></MagicCard></>}
function Cosplay({data}){return <><header><div><small>Make magic piece by piece</small><h1>Cosplay</h1></div></header><SectionHeader title="Projects"/><MagicCard><div className="cosplay-summary"><div className="project-icon">✿</div><div><b>{data.cosplay.name}</b><small>Galaxy Con · 3 pieces</small></div></div><ProgressSpell value={data.cosplay.progress} label="Project progress"/></MagicCard><SectionHeader title="Project detail"/><MagicCard>{data.cosplay.pieces.map(p=><div className="piece" key={p.name}><div><b>{p.name}</b><small>{p.mode} · ${p.cost} · due {p.due}</small></div><span className={'status '+p.status.toLowerCase().replace(' ','-')}>{p.status}</span></div>)}</MagicCard></>}
function More(){return <><header><div><small>Your wider world</small><h1>More</h1></div></header><p className="muted">Future modules, ready when you are.</p><div className="placeholder-grid">{['Conventions','Travel','Creator HQ','Wellness','Routines','Yuu-Kun','Settings'].map(x=><MagicCard key={x}><span className="placeholder-icon">✦</span><b>{x}</b><small>Coming later</small></MagicCard>)}</div></>}
function MoneyNew({data,setData}){const m=data.money,rem=Math.max(0,(m.weeklyGoal||0)-(m.weeklyEarned||0)),[kind,setKind]=useState(null),[amount,setAmount]=useState(''),[source,setSource]=useState('Gig'),[bucket,setBucket]=useState('life'),[available,setAvailable]=useState(m.availableToday),[editing,setEditing]=useState(false);const save=p=>setData({...data,money:{...m,...p}});const submit=e=>{e.preventDefault();const n=Number(amount);if(!Number.isFinite(n)||n<=0)return;const tx={id:Date.now(),kind,amount:n,source:kind==='earned'?source:bucket,date:localDate()};kind==='earned'?save({weeklyEarned:m.weeklyEarned+n,earnedToday:Number(m.earnedToday||0)+n,transactions:[tx,...m.transactions]}):save({availableToday:Math.max(0,m.availableToday-n),buckets:{...m.buckets,[bucket]:Math.max(0,m.buckets[bucket]-n)},transactions:[tx,...m.transactions]});setKind(null);setAmount('')};const w=m.workWindows?.[0];return <><header><div><small>Make the week work for you.</small><h1>Money</h1></div></header><MagicCard className="money-hero"><div className="money-hero-label"><small>Available today</small><button className="hero-edit" onClick={()=>{if(editing){const n=Number(available);if(Number.isFinite(n)&&n>=0)save({availableToday:n})}setEditing(!editing)}}>{editing?'Save':'Edit'}</button></div>{editing?<input className="hero-input" type="number" min="0" step=".01" value={available} onChange={e=>setAvailable(e.target.value)}/>:<strong>{'$'}{Number(m.availableToday).toFixed(2)}</strong>}<p>Usable money for today</p></MagicCard><SectionHeader title="Money buckets"/><div className="bucket-grid">{[['life','Life'],['con','Con'],['fun','Fun']].map(([id,l])=><div className="bucket" key={id}><small>{l}</small><b>{'$'}{Number(m.buckets[id]||0).toFixed(0)}</b></div>)}</div><SectionHeader title="Weekly Money Mission"/><MagicCard className="mission-card"><div className="mission-top"><div><small>Goal</small><b>{'$'}{m.weeklyGoal}</b></div><div><small>Earned</small><b>{'$'}{m.weeklyEarned}</b></div><label>Goal<input className="inline-input" type="number" min="1" value={m.weeklyGoal} onChange={e=>{const n=Number(e.target.value);if(Number.isFinite(n)&&n>0)save({weeklyGoal:n})}}/></label></div><ProgressSpell value={m.weeklyGoal?m.weeklyEarned/m.weeklyGoal*100:0} label={'$'+rem+' remaining · $'+Math.ceil(rem/Math.max(1,m.daysRemaining))+'/day suggested'}/><div className="money-stats"><div><small>Earned today</small><b>{'$'}{m.earnedToday||0}</b></div><div><small>Days left</small><b>{m.daysRemaining}</b></div></div></MagicCard><SectionHeader title="Quick Log"/><MagicCard><div className="log-actions"><button className="primary" onClick={()=>setKind('earned')}>+ Earned</button><button className="secondary" onClick={()=>setKind('spent')}>− Spent</button></div>{kind&&<form className="log-form" onSubmit={submit}><label>Amount<input required type="number" min=".01" step=".01" inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)}/></label>{kind==='earned'?<label>Source<select value={source} onChange={e=>setSource(e.target.value)}><option>Gig</option><option>Creator</option><option>Other</option></select></label>:<label>Bucket<select value={bucket} onChange={e=>setBucket(e.target.value)}><option value="life">Life</option><option value="con">Con</option><option value="fun">Fun</option></select></label>}<button className="primary">Save log</button></form>}</MagicCard><SectionHeader title="Good Work Window"/><MagicCard><div className="window-row"><div><b>{w?.start||'17:00'} – {w?.end||'21:00'}</b><small>{w?.days||'Weekdays'} · {w?.active===false?'Skipped today':"Today's window"}</small></div><button className="tiny-action" onClick={()=>save({workWindows:[{...(w||{}),active:!w?.active}]})}>Toggle today</button></div><button className="primary">Start work</button></MagicCard><SectionHeader title="Upcoming Money"/><MagicCard>{m.upcoming.slice(0,3).map(x=><div className="upcoming-row" key={x.id}><b>{x.title}</b><small>{'$'}{x.amount} · {x.due}</small></div>)}</MagicCard><SectionHeader title="Recent Activity"/><MagicCard>{m.transactions.slice(0,4).map(x=><div className="activity-row" key={x.id}><b>{x.kind==='earned'?'+':'−'}{'$'}{x.amount}</b><small>{x.source} · {x.date}</small></div>)}</MagicCard></>}
function CosplayNew({data,setData}){const c=data.cosplay,projects=c.projects||[],active=projects.find(p=>p.id===c.activeId)||projects[0], [filter,setFilter]=useState('All'),[selected,setSelected]=useState(null),[edit,setEdit]=useState(null),[pieceEdit,setPieceEdit]=useState(null);const saveProjects=ps=>{const a=ps.find(p=>p.id===c.activeId)||ps[0];setData({...data,cosplay:{...c,projects:ps,activeId:a?.id,name:a?.name,progress:a?.progress,pieces:a?.pieces}})};const blank={name:'',source:'',state:'Planning',targetEvent:'',targetDate:'',budget:0,notes:'',pieces:[]};const visible=projects.filter(p=>filter==='All'||(filter==='Ready'&&p.state==='Ready')||(filter==='Wishlist'&&p.state==='Wishlist')||(filter==='Making'&&p.pieces.some(x=>x.method==='Make'))||(filter==='Buying'&&p.pieces.some(x=>x.method==='Buy')));const progress=p=>p.pieces.length?Math.round(p.pieces.filter(x=>x.status==='Ready').length/p.pieces.length*100):Number(p.progress||0);const submitProject=e=>{e.preventDefault();const p={...edit,id:edit.id||Date.now(),budget:Number(edit.budget)||0,pieces:edit.pieces||[]};saveProjects(edit.id?projects.map(x=>x.id===p.id?p:x):[...projects,p]);setEdit(null)};const submitPiece=e=>{e.preventDefault();const p=projects.find(x=>x.id===selected.id),piece={...pieceEdit,id:pieceEdit.id||Date.now(),cost:Number(pieceEdit.cost)||0};saveProjects(projects.map(x=>x.id===p.id?{...x,pieces:pieceEdit.id?x.pieces.map(z=>z.id===piece.id?piece:z):[...x.pieces,piece]}:x));setPieceEdit(null)};if(selected){const p=projects.find(x=>x.id===selected.id)||active,ready=p.pieces.filter(x=>x.status==='Ready').length,total=p.pieces.reduce((n,x)=>n+Number(x.cost||0),0),next=p.pieces.filter(x=>x.status!=='Ready'&&x.due).sort((a,b)=>a.due.localeCompare(b.due))[0];return <><header><div><small>{p.source||'Project detail'}</small><h1>{p.name}</h1></div><button className="secondary" onClick={()=>setSelected(null)}>Back</button></header><MagicCard><b>{p.targetEvent||'No event set'}{p.targetDate&&' · '+p.targetDate}</b><ProgressSpell value={progress(p)} label={ready+' of '+p.pieces.length+' pieces ready'}/><p className="muted">Budget estimate: ${total}{p.budget?' of $'+p.budget+' target':''}</p>{next&&<p className="next-piece">Next: {next.name} · due {next.due}</p>}</MagicCard><SectionHeader title="Pieces"/><MagicCard>{p.pieces.map(x=><div className="piece" key={x.id}><div><b>{x.name}</b><small>{x.method} · ${x.cost} · {x.status}{x.due?' · '+x.due:''}</small></div><button className="tiny-action" onClick={()=>setPieceEdit({...x})}>Edit</button></div>)}{!p.pieces.length&&<p className="muted">Nothing listed yet.</p>}<button className="primary" onClick={()=>setPieceEdit({name:'',method:'Make',cost:0,status:'Planning',due:'',link:'',notes:''})}>+ Add Piece</button></MagicCard><button className="secondary" onClick={()=>setEdit({...p})}>Edit project</button>{pieceEdit&&<MagicCard><form className="log-form" onSubmit={submitPiece}><label>Name<input required value={pieceEdit.name} onChange={e=>setPieceEdit({...pieceEdit,name:e.target.value})}/></label><label>Method<select value={pieceEdit.method} onChange={e=>setPieceEdit({...pieceEdit,method:e.target.value})}><option>Make</option><option>Buy</option><option>Commission</option></select></label><label>Cost<input type="number" min="0" step=".01" value={pieceEdit.cost} onChange={e=>setPieceEdit({...pieceEdit,cost:e.target.value})}/></label><label>Status<select value={pieceEdit.status} onChange={e=>setPieceEdit({...pieceEdit,status:e.target.value})}>{['Planning','In Progress','Ordered','Arrived','Ready','Repair'].map(s=><option key={s}>{s}</option>)}</select></label><label>Due date<input type="date" value={pieceEdit.due||''} onChange={e=>setPieceEdit({...pieceEdit,due:e.target.value})}/></label><label>Link<input value={pieceEdit.link||''} onChange={e=>setPieceEdit({...pieceEdit,link:e.target.value})}/></label><label>Notes<input value={pieceEdit.notes||''} onChange={e=>setPieceEdit({...pieceEdit,notes:e.target.value})}/></label><button className="primary">Save piece</button></form></MagicCard>}</>};return <><header><div><small>Build it piece by piece ✨</small><h1>Cosplay</h1></div><button className="primary" onClick={()=>setEdit(blank)}>+ New Cosplay</button></header><div className="filter-chips">{['All','Making','Buying','Ready','Wishlist'].map(x=><button className={filter===x?'selected':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div>{visible.map(p=>{const next=p.pieces.filter(x=>x.status!=='Ready'&&x.due).sort((a,b)=>a.due.localeCompare(b.due))[0];return <MagicCard key={p.id} className={p.id===c.activeId?'active-project':''}><button className="project-card-button" onClick={()=>setSelected(p)}><span className="project-icon">✿</span><b>{p.name}</b><small>{p.source||'Cosplay project'} · {p.targetEvent||'No event'}</small><ProgressSpell value={progress(p)} label={p.pieces.length+' pieces · '+p.state}/><small>{next?'Next: '+next.name+' · '+next.due:'Nothing pending'}</small></button><button className="tiny-action" onClick={()=>{saveProjects(projects.map(x=>({...x,activeId:undefined})));saveProjects(projects.map(x=>x.id===p.id?x:x));setData({...data,cosplay:{...c,projects,activeId:p.id,name:p.name,progress:progress(p),pieces:p.pieces}})}}>{p.id===c.activeId?'Current':'Make current'}</button></MagicCard>})}{!visible.length&&<MagicCard><b>No cosplays yet ✨</b><p className="muted">Start with the character living rent-free in your head.</p></MagicCard>}{edit&&<MagicCard><form className="log-form" onSubmit={submitProject}><label>Name<input required value={edit.name} onChange={e=>setEdit({...edit,name:e.target.value})}/></label><label>Source<input value={edit.source||''} onChange={e=>setEdit({...edit,source:e.target.value})}/></label><label>State<select value={edit.state} onChange={e=>setEdit({...edit,state:e.target.value})}>{['Wishlist','Planning','In Progress','Ready','Archived'].map(s=><option key={s}>{s}</option>)}</select></label><label>Target event<input value={edit.targetEvent||''} onChange={e=>setEdit({...edit,targetEvent:e.target.value})}/></label><label>Target date<input type="date" value={edit.targetDate||''} onChange={e=>setEdit({...edit,targetDate:e.target.value})}/></label><label>Budget target<input type="number" min="0" value={edit.budget||0} onChange={e=>setEdit({...edit,budget:e.target.value})}/></label><label>Notes<input value={edit.notes||''} onChange={e=>setEdit({...edit,notes:e.target.value})}/></label><button className="primary">Save project</button></form></MagicCard>}</>}
function App(){const [screen,setScreen]=useState('home'),[data,setData,cloud]=useStore();const page=screen==='home'?<HomeCommand {...{data,setData,setScreen}}/>:screen==='plan'?<TaskCrud {...{data,setData}}/>:screen==='money'?<MoneyCrud {...{data,setData,setScreen}}/>:screen==='cosplay'?<CosplayCrud {...{data,setData}}/>:screen==='conventions'?<ConventionCrud {...{data,setData,setScreen}}/>:screen==='travel'?<Travel {...{data,setData}}/>:screen==='creator'?<CreatorHQ {...{data,setData}}/>:screen==='wellness'?<Wellness {...{data,setData,setScreen}}/>:screen==='routines'?<Routines {...{data,setData}}/>:screen==='reminders'?<Reminders {...{data,setData,setScreen}}/>:screen==='yuu'?<YuuChat {...{data,setData,setScreen}}/>:screen==='settings'?<Settings {...{data,setData,cloud}}/>:<MoreFixed setScreen={setScreen}/>;return <AppShell {...{screen,setScreen}}>{page}</AppShell>}
createRoot(document.getElementById('root')).render(<App/>);
function HomeFixed({data,setData,setScreen}){const project=getActiveCosplay(data.cosplay),progress=getCosplayProgress(project),next=getNextCosplayPiece(project),today=localDate(),tasks=data.tasks.filter(t=>t.date===today),m=data.money;return <><header><div><small>{new Intl.DateTimeFormat(undefined,{weekday:'long',month:'long',day:'numeric'}).format(new Date())}</small><h1>Home</h1></div></header><YuuBubble>Yare yare. {tasks.filter(t=>!t.done).length} things today.</YuuBubble><SectionHeader title="Today"/><MagicCard><div className="task-list">{tasks.map(t=><TaskRow key={t.id} task={t} onToggle={()=>setData({...data,tasks:data.tasks.map(x=>x.id===t.id?{...x,done:!x.done}:x)})}/>)}</div></MagicCard><SectionHeader title="Money Today"/><MagicCard className="money-home" onClick={()=>setScreen('money')}><strong>${m.availableToday.toFixed(2)}</strong></MagicCard><SectionHeader title="Current Cosplay"/><MagicCard className="cosplay-home" onClick={()=>setScreen('cosplay')}>{project?<><div className="cosplay-summary"><div className="project-icon">✿</div><div><b>{project.name}</b><small>{project.targetEvent||'Cosplay project'}{project.targetDate?' · '+project.targetDate:''}</small></div></div><ProgressSpell value={progress} label="Piece readiness"/>{next&&<p className="next-piece">Next: {next.name}{next.due?' · due '+next.due:''}</p>}</>:<div className="empty-cosplay"><b>No active cosplay yet ✨</b><small>Choose a project in Cosplay to show it here.</small></div>}</MagicCard><SectionHeader title="Coming Up"/><MagicCard className="coming-up">{(()=>{const c=getNearestConvention(data.conventions?.items);return c?<><b>{c.name}</b><small>{countdown(c)} · {c.startDate}{c.location?" · "+c.location:""}</small></>:<b>No upcoming conventions yet.</b>})()}</MagicCard></>}
function MoreFixed({setScreen}){const links=[['creator','Creator HQ'],['wellness','Wellness'],['routines','Routines'],['reminders','Reminders'],['yuu','Yuu-Kun'],['settings','Settings']];return <><header><div><small>Your wider world</small><h1>More</h1></div></header><div className="placeholder-grid"><MagicCard><button className="project-card-button" onClick={()=>setScreen('conventions')}><b>Conventions</b><small>Open command center</small></button></MagicCard><MagicCard><button className="project-card-button" onClick={()=>setScreen('travel')}><b>Travel</b><small>Open trip command center</small></button></MagicCard>{links.map(([id,label])=><MagicCard key={id}><button className="project-card-button" onClick={()=>setScreen(id)}><b>{label}</b><small>Open module</small></button></MagicCard>)}</div></>}
function Convention({data,setData}){const [chosen,setChosen]=useState(null),[tab,setTab]=useState('overview'),[edit,setEdit]=useState(null),items=data.conventions.items||[],c=items.find(x=>x.id===chosen),cos=data.cosplay.projects||[];const save=next=>setData({...data,conventions:{...data.conventions,items:next}});const blank={name:'',venue:'',location:'',startDate:'',endDate:'',status:'Upcoming',budgetEstimate:0,cosplayIds:[],schedule:[],checklist:[],packing:[],milestones:{},content:{target:0,done:0},conDay:{}};const submit=e=>{e.preventDefault();const n={...edit,id:edit.id||Date.now(),budgetEstimate:Number(edit.budgetEstimate)||0};save(edit.id?items.map(x=>x.id===n.id?n:x):[...items,n]);setEdit(null);setChosen(n.id)};if(!c)return <><header><div><small>More · Command center</small><h1>Conventions</h1></div><button className="primary" onClick={()=>setEdit(blank)}>+ New Convention</button></header>{items.map(x=><MagicCard key={x.id}><button className="project-card-button" onClick={()=>setChosen(x.id)}><b>{x.name}</b><small>{x.startDate||'Date TBD'} · {x.location||x.venue||'Location TBD'}</small><strong>{countdown(x)}</strong></button></MagicCard>)}{!items.length&&<MagicCard><b>No missions on the board yet ✨</b></MagicCard>}{edit&&<ConventionForm edit={edit} setEdit={setEdit} cos={cos} submit={submit}/>}</>;const linked=cos.filter(p=>c.cosplayIds?.includes(p.id)),days=Math.ceil((new Date((c.startDate||localDate())+'T12:00:00')-new Date(localDate()+'T12:00:00'))/86400000),phase=days<=2?'2-day':days<=7?'7-day':days<=14?'14-day':'30-day',milestones=phase==='14-day'?['Confirm contacts','Confirm shoes','Confirm props','Test full cosplay']:phase==='7-day'?['Start packing','Prepare tech','Confirm schedule']:phase==='2-day'?['Final cosplay check','Pack essentials','Save confirmations']:['Check cosplay progress','Confirm content ideas'];const toggle=x=>save(items.map(y=>y.id===c.id?{...y,milestones:{...y.milestones,[x]:!y.milestones?.[x]}}:y));return <><header><div><small>More · Conventions</small><h1>{c.name}</h1></div><button className="secondary" onClick={()=>setChosen(null)}>Back</button></header><div className="segmented">{['overview','schedule','prep','con day'].map(x=><button className={tab===x?'selected':''} onClick={()=>setTab(x)} key={x}>{x}</button>)}</div>{tab==='overview'&&<><MagicCard className="con-hero"><small>Countdown</small><strong>{countdown(c)}</strong><p>{c.startDate||'Date TBD'} · {c.location||c.venue}</p></MagicCard><MagicCard><b>Linked Cosplays</b>{linked.map(p=><div className="upcoming-row" key={p.id}><b>{p.name}</b><small>{p.pieces?.filter(x=>x.status==='Ready').length||0}/{p.pieces?.length||0} ready</small></div>)}<p className="muted">Budget estimate: ${c.budgetEstimate||0}</p></MagicCard><button className="secondary" onClick={()=>setEdit(c)}>Edit convention</button></>}{tab==='prep'&&<><SectionHeader title={phase+' milestone'}/><MagicCard>{milestones.map(x=><label className="task-row" key={x}><input type="checkbox" checked={Boolean(c.milestones?.[x])} onChange={()=>toggle(x)}/><span>{x}</span></label>)}<QuickAdd onAdd={title=>save(items.map(x=>x.id===c.id?{...x,checklist:[...(x.checklist||[]),{id:Date.now(),title,done:false}]}:x))}/></MagicCard><SectionHeader title="Packing"/><MagicCard><QuickAdd onAdd={title=>save(items.map(x=>x.id===c.id?{...x,packing:[...(x.packing||[]),{id:Date.now(),title,packed:false}]}:x))}/>{(c.packing||[]).map(x=><label className="task-row" key={x.id}><input type="checkbox" checked={x.packed} onChange={()=>save(items.map(y=>y.id===c.id?{...y,packing:y.packing.map(z=>z.id===x.id?{...z,packed:!z.packed}:z)}:y))}/><span>{x.title}</span></label>)}</MagicCard></>}{tab==='schedule'&&<MagicCard>{(c.schedule||[]).slice().sort((a,b)=>(a.date+a.time).localeCompare(b.date+b.time)).map(x=><div className="upcoming-row" key={x.id}><b>{x.title}</b><small>{x.date} · {x.time}</small></div>)}<QuickAdd onAdd={title=>save(items.map(x=>x.id===c.id?{...x,schedule:[...(x.schedule||[]),{id:Date.now(),title,date:c.startDate,time:'12:00'}]}:x))}/></MagicCard>}{tab==='con day'&&<MagicCard><b>Con Day Mode</b><p className="muted">{c.name} · {localDate()}</p><label className="task-row">Ate something<input type="checkbox" checked={Boolean(c.conDay?.ate)} onChange={()=>save(items.map(x=>x.id===c.id?{...x,conDay:{...x.conDay,ate:!x.conDay?.ate}}:x))}/></label><label className="task-row">Water check<input type="checkbox" checked={Boolean(c.conDay?.water)} onChange={()=>save(items.map(x=>x.id===c.id?{...x,conDay:{...x.conDay,water:!x.conDay?.water}}:x))}/></label><p className="muted">TikToks: {c.content?.done||0}/{c.content?.target||0}</p></MagicCard>}{edit&&<ConventionForm edit={edit} setEdit={setEdit} cos={cos} submit={submit}/>}</>}
function ConventionForm({edit,setEdit,cos,submit}){return <MagicCard><form className="log-form" onSubmit={submit}><label>Name<input required value={edit.name} onChange={e=>setEdit({...edit,name:e.target.value})}/></label><label>Venue<input value={edit.venue||''} onChange={e=>setEdit({...edit,venue:e.target.value})}/></label><label>Location<input value={edit.location||''} onChange={e=>setEdit({...edit,location:e.target.value})}/></label><label>Start date<input required type="date" value={edit.startDate} onChange={e=>setEdit({...edit,startDate:e.target.value})}/></label><label>End date<input type="date" value={edit.endDate||''} onChange={e=>setEdit({...edit,endDate:e.target.value})}/></label><label>Budget estimate<input type="number" min="0" value={edit.budgetEstimate||0} onChange={e=>setEdit({...edit,budgetEstimate:e.target.value})}/></label><label>Linked cosplays<select multiple value={edit.cosplayIds||[]} onChange={e=>setEdit({...edit,cosplayIds:Array.from(e.target.selectedOptions,o=>Number(o.value))})}>{cos.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><button className="primary">Save convention</button></form></MagicCard>}
















