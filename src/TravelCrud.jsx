import React,{useState} from 'react';
const Card=({children})=><section className="card">{children}</section>;const fields={flights:['airline','flightNumber','from','to','departureDate','departureTime','arrivalTime','confirmation','terminalGate','seat','notes'],stays:['name','address','checkInDate','checkOutDate','confirmation','notes'],transport:['type','date','time','pickup','destination','confirmation','notes'],itinerary:['title','date','time','location','type','notes'],packing:['title','category','packed'],confirmations:['label','value','notes'],foodSpots:['name','location','notes'],postTripReset:['title','done'],packingTemplates:['title','category']};
export default function TravelCrud({data,setData}){const st=data.travel||{activeId:null,trips:[],packingTemplates:[]},trips=st.trips||[],[id,setId]=useState(st.activeId),[edit,setEdit]=useState(null),[child,setChild]=useState(null),trip=trips.find(x=>x.id===id),saveTrips=next=>setData({...data,travel:{...st,trips:next,activeId:id}}),saveTrip=t=>saveTrips(trips.map(x=>x.id===t.id?t:x)),removeTrip=t=>{if(!window.confirm('Delete trip?'))return;const next=trips.filter(x=>x.id!==t.id),active=next[0]?.id||null;setData({...data,travel:{...st,trips:next,activeId:active}});setId(active)},saveChild=(key,v)=>{const n={...v,id:v.id||Date.now()};saveTrip({...trip,[key]:v.id?(trip[key]||[]).map(x=>x.id===n.id?n:x):[...(trip[key]||[]),n]});setChild(null)},removeChild=(key,x)=>{if(window.confirm('Delete this travel item?'))saveTrip({...trip,[key]:(trip[key]||[]).filter(y=>y.id!==x.id)})};if(!trip)return <><header><h1>Travel</h1><button className="primary" onClick={()=>setEdit({name:'',destination:'',startDate:'',endDate:'',status:'Planning',linkedConventionId:null,budgetEstimate:0,notes:'',emergencyInfo:'',id:null})}>+ New Trip</button></header>{trips.map(x=><Card key={x.id}><b>{x.name}</b><small>{x.destination} · {x.startDate||'Dates TBD'}</small><div className="card-actions"><button className="edit-action" onClick={()=>setEdit({...x})}>Edit</button><button className="edit-action" onClick={()=>setId(x.id)}>Open</button><button className="delete-action" onClick={()=>removeTrip(x)}>Delete</button></div></Card>)}{!trips.length&&<Card><b>No trips yet ✨</b></Card>}{edit&&<TripForm value={edit} setValue={setEdit} save={v=>{const n={...v,id:v.id||Date.now()};setData({...data,travel:{...st,trips:v.id?trips.map(x=>x.id===n.id?n:x):[...trips,n],activeId:n.id}});setEdit(null);setId(n.id)}}/>}</>;return <><header><div><small>More · Travel</small><h1>{trip.name}</h1><small>{trip.destination}</small></div><button className="secondary" onClick={()=>setId(null)}>Back</button></header><Card><div className="card-actions"><button className="edit-action" onClick={()=>setEdit({...trip})}>Edit trip</button><button className="delete-action" onClick={()=>removeTrip(trip)}>Delete trip</button></div></Card>{Object.keys(fields).filter(k=>k!=='packingTemplates').map(key=><ChildSection key={key} name={key} rows={trip[key]||[]} setChild={setChild} remove={removeChild}/>) }<Card><h2>Packing templates</h2>{(st.packingTemplates||[]).map(x=><div className="activity-row" key={x.id||x.title}><span>{x.title||x}</span><button className="delete-action" onClick={()=>{if(window.confirm('Delete template?'))setData({...data,travel:{...st,packingTemplates:(st.packingTemplates||[]).filter(y=>(y.id||y)!==(x.id||x))}})}}>Delete</button></div>)}<button className="primary" onClick={()=>{const title=window.prompt('Template item');if(title)setData({...data,travel:{...st,packingTemplates:[...(st.packingTemplates||[]),{id:Date.now(),title}]}})}}>+ Template</button></Card>{edit&&<TripForm value={edit} setValue={setEdit} save={v=>{saveTrip({...v,id:trip.id});setEdit(null)}}/>}{child&&<ChildForm kind={child.kind} value={child} setValue={setChild} save={v=>saveChild(child.kind,v)}/>}</>}
function ChildSection({name,rows,setChild,remove}){return <Card><h2>{name}</h2>{rows.map(x=><div className="activity-row" key={x.id}><span>{x.title||x.name||x.airline||x.label||x.type||'Item'}</span><span><button className="edit-action" onClick={()=>setChild({kind:name,...x})}>Edit</button><button className="delete-action" onClick={()=>remove(name,x)}>Delete</button></span></div>)}<button className="primary" onClick={()=>setChild({kind:name})}>+ Add</button></Card>}
function TripForm({value,setValue,save}){return <Card><form className="log-form" onSubmit={e=>{e.preventDefault();save(value)}}>{['name','destination','startDate','endDate','status','budgetEstimate','notes','emergencyInfo'].map(k=><label key={k}>{k}<input type={k.includes('Date')?'date':k==='budgetEstimate'?'number':'text'} value={value[k]||''} onChange={e=>setValue({...value,[k]:e.target.value})}/></label>)}<button className="primary">Save</button><button type="button" className="secondary" onClick={()=>setValue(null)}>Cancel</button></form></Card>}
function ChildForm({kind,value,setValue,save}){return <Card><form className="log-form" onSubmit={e=>{e.preventDefault();save(value)}}>{(fields[kind]||[]).map(k=><label key={k}>{k}<input type={k.toLowerCase().includes('date')?'date':k.toLowerCase().includes('time')?'time':k==='amount'?'number':'text'} value={value[k]||''} onChange={e=>setValue({...value,[k]:e.target.value})}/></label>)}<button className="primary">Save</button><button type="button" className="secondary" onClick={()=>setValue(null)}>Cancel</button></form></Card>}

